TODO: move somewhere else for final version
;NormAns etc should work on any register
;Need better system with pointers set aside
;Any FP register can be args or dest
	;either (R0),Y or LDX R0 then 0,X
	;X is easier to do offset within same word
	
TODO: this might help reduce size a lot
;fp register based VM (see google doc)

	